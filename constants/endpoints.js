const BASE_URL = 'http://jsonplaceholder.typicode.com';

const ENDPOINTS = {
  TODOS: '/todos',
  POSTS: '/posts',
  COMMENTS: '/comments',
  ALBUMS: '/albums',
  PHOTOS: '/photos',
  USERS: '/users'
};

module.exports = { BASE_URL, ENDPOINTS };
